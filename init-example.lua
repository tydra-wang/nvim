require("wang").setup {
    spec = {
        -- enable all language
        -- { import = "plugins.lang" },

        -- enable only golang plugins
        -- use "ls -l ~/.config/nvim/lua/plugins/lang" to see all supported language
        -- { import = "plugins.lang.go" },
    },
}

-- set colorscheme
-- vim.cmd.colorscheme "catppuccin"
